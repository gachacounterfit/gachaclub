package {
	import flash.display.BitmapData;
	
	public dynamic class shadowramunadeboss extends BitmapData {
		 
		
		public function shadowramunadeboss(w:int = 539, h:int = 500) {
			super(w,h);
		}
	}
}
